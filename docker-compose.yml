services:
  x-market-watch:
    build: .
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    ports:
      - "8787:8787"
    command: ["x-market-watch", "web"]
    restart: unless-stopped
