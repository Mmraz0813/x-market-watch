services:
  x-market-watch:
    build: .
    environment:
      X_MARKET_WATCH_ENV_PATH: data/.env
    volumes:
      - ./data:/app/data
    ports:
      - "8787:8787"
    command: ["x-market-watch", "web"]
    restart: unless-stopped
